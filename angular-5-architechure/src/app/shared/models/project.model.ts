export class Project   {
    status: boolean;
    imageData:[imageData];
    albumData:[albumData];
  }

  class imageData{
    id: string;
    name: string;
    url: string;
  }

  class albumData{
    id: string;
    name: string;
    imageCount: string;
  }